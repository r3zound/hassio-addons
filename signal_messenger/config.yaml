name: Signal Messenger
url: https://gh-proxy.org/github.com/haberda/signal-addon
version: 0.100.1
slug: signal_messenger
arch:
  - aarch64
  - amd64
  - armv7
description: '[老王杂谈说]：null'
startup: services
ports:
  8080/tcp: 8080
ports_description:
  8080/tcp: Rest-api port
options:
  mode: list(normal|native|json-rpc)
  AUTO_RECEIVE: true
  SIGNAL_CLI_CMD_TIMEOUT: 60
map:
  - addon_config:rw
schema:
  mode: list(normal|native|json-rpc)
  AUTO_RECEIVE: bool
  SIGNAL_CLI_CMD_TIMEOUT: int
environment:
  SIGNAL_CLI_CONFIG_DIR: /config
image: ghcr.nju.edu.cn/haberda/signal_messenger/{arch}
